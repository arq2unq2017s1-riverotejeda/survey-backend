package unq.utils;

import com.librato.metrics.client.Duration;
import com.librato.metrics.client.LibratoClient;

import static java.util.concurrent.TimeUnit.SECONDS;

public class Librato {

   /* public static final LibratoClient client;

    static {
        client = LibratoClient.builder("tejedamarcia@gmail.com", "ef7823a99f82623bbfccb404bb3dcfe4224216524e1e745182fb71dee8a91d08")
                .setConnectTimeout(new Duration(5, SECONDS))
                .setReadTimeout(new Duration(5, SECONDS))
                .setAgentIdentifier("sb2")
                .build();
    }*/

}
